server:
  port: 8094

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marcklinux/microservices-store
          search-paths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t


